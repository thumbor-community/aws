# Constraints to minimize pip backtracking and ensure compatibility
# AWS stack compatible with aiobotocore==2.23.0
aiobotocore==2.23.0
boto3==1.38.27
botocore==1.38.27
s3transfer==0.13.0
jmespath==0.10.0
docutils==0.15.2
python-dateutil==2.8.2
urllib3==1.25.11

# Thumbor 7 alpha compatible stack (kept broad otherwise)
# Pinning thumbor avoids heavy backtracking via pillow/tornado
# Allow a newer thumbor compatible with Pillow; don't overconstrain
thumbor>=7.3,<8

# Test/runtime utilities commonly resolved with wide ranges
idna==3.10
certifi==2025.7.14

# Ensure modern build backend to parse modern extras in dependencies (e.g., thumbor)
setuptools>=75.9.1
wheel>=0.45.1
pip>=25.2

# Use a prebuilt wheel for Pillow to avoid compiling native deps
# Pillow wheel version compatible with thumbor>=7.3
Pillow==10.4.0

